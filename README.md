# gayao
不知道写什么
# 我的项目

## 项目简介
这是我在[课程名称]考核中提交的项目作品，包含个人简介、个人网站、小游戏（贪吃蛇/2048/扫雷）以及进阶挑战。

## 作者
- 姓名：邓萧阳
- 专业：集成电路设计及集成系统
- GitHub：Daniel-x-young

## 项目结构.
- README.md # 项目说明文档
- docs/ # 个人简介PDF
- index/ # 个人网站源码
- game/ # 小游戏（单HTML文件）
- circuits/ # 进阶挑战：PySpice电路仿真
- chip/ # 进阶挑战：Croc RISC-V项目
## 进度记录
- [ ] 环境搭建完成
- [ ] 个人简介PDF
- [ ] 个人网站
- [ ] 小游戏阶段一
- [ ] 小游戏阶段二
- [ ] 新增功能
- [ ] 进阶挑战（可选）
### 分支（Branch）
分支就像在一本书的主线剧情之外，另起一页写一个番外篇。你可以在番外篇里自由修改内容，而主线剧情（主分支 main）不受影响。这样多人协作或尝试新功能时，不会破坏稳定版本。

### 合并（Merge）
合并就是把番外篇的内容整合回主线剧情中。当你确认番外篇的修改没有问题后，通过 `git merge` 操作将分支的更改合并到主分支，让两部分内容合二为一。
## 阶段一：能自己玩的贪吃蛇

### 实现方式
我采用纯前端单文件（HTML/CSS/JS）的方式，用Canvas绘制游戏区域。
- 蛇用数组存储坐标，每150ms移动一步
- 键盘方向键控制方向，通过检查当前方向防止180°掉头
- 食物随机生成在空白格子上
- 撞墙或撞自身触发游戏结束

### 代码逻辑
1. **蛇的移动与变长**：蛇的身体是一个数组，第0个元素是蛇头，最后一个元素是蛇尾。每次移动时，我计算新蛇头的位置并插入到数组最前面，然后根据是否吃到食物来决定是否移除蛇尾。如果吃到了食物，蛇尾保留（长度+1）；如果没吃到，移除蛇尾（长度不变）。方向用 x 和 y 的偏移量表示，比如 {x:1, y:0} 表示向右移动一格。
2. **方向控制**：方向控制的关键是防止蛇180°掉头撞到自己。例如蛇当前正在向上移动（dy = -1），如果玩家按下"下"键，蛇会立刻掉头向下，导致蛇头撞到自己的身体。解决方案是：在改变方向前检查当前移动方向，只有新方向与当前方向相反时才忽略此次按键。代码中用 dy !== 1 判断"当前不是向下"，以此允许向上移动。
3. **碰撞检测**：游戏结束分两种情况：一是蛇头超出画布边界（撞墙）；二是蛇头与自己的身体（从第2节开始检查）坐标重叠（撞自身）。注意检查自身时要跳过蛇头本身（索引0），否则会误判为"蛇头撞到自己"导致游戏立即结束。

### 给AI的提示词
- 第1轮：用纯HTML/CSS写一个贪吃蛇游戏，要求：
1. 单HTML文件，所有代码都在一个文件中
2. 使用Canvas绘制，画布大小400×400像素
3. 网格20×20，每个格子20×20像素
4. 蛇初始在画布中央，长度为3，方向向右
5. 键盘方向键控制移动，不能反向移动
6. 食物随机生成（红色方块）
7. 吃到食物蛇变长，得分+1
8. 撞墙或撞自身则游戏结束
9. 页面顶部显示当前得分
10. 游戏结束后按空格键重新开始
请生成完整代码。
- 第2轮：基于上面的代码，做以下修改：
1. 移动速度改为每150ms移动一步（用setInterval）
2. 食物生成时确保不在蛇身上
3. 改为深色背景，蛇身为渐变绿色，食物为红色发光效果
4. 增加"游戏进行中" / "游戏结束" 的状态提示
5. 增加一个"重新开始"按钮
- 第3轮：在游戏中增加计分制功能：
1. 用localStorage保存历史最高分
2. 页面加载时显示历史最高分
3. 游戏结束时，如果当前得分高于历史最高分，则更新
4. 显示"当前得分"和"历史最高分"
5. 增加一个"重置记录"按钮，清空历史最高分
### AI玩
-第1轮
1.在现有贪吃蛇代码中增加AI自动演示功能。添加一个「AI演示」按钮，点击后蛇自动移动寻找食物。使用BFS广度优先搜索算法计算蛇头到食物的最短路径，然后沿路径移动。界面上显示当前已吃食物数量。
2.AI蛇经常把自己围死。增加安全检测：在决定下一步之前，模拟走这一步后，检查蛇头是否还能到达蛇尾。如果不能到达，说明这步会封死自己，要换其他方向。优先保证生存，其次才是吃食物
3.将AI演示与玩家记录分开并重新排版
### AI给过但我查证后认为是错误的信息
> AI曾说"用 `while (true)` 循环来控制游戏主循环"，但我查证后发现浏览器中无限循环会阻塞UI渲染，应该用 `setInterval` 或 `requestAnimationFrame`。我改用了 `setInterval(moveSnake, 150)`。